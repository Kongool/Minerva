# Build output
bin/
obj/
.vs/
*.user
*.suo

# Generated tooling artifacts (reproducible from game data)
tools/__pycache__/
tools/action_names.tsv

# Session-local Claude agent config (settings, skills, local replays) — not part of the plugin
.claude/

# Local reference docs (IPC specs, integration notes) — not part of the shipped plugin
docs/

# Only README.md is tracked in the repo; other markdown (PLAN.md, notes) stays local
*.md
!README.md
